apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-config-longhorn
  namespace: calibre-web
  labels:
    app.kubernetes.io/name: calibre-web
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-books-longhorn
  namespace: calibre-web
  labels:
    app.kubernetes.io/name: calibre-web
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
