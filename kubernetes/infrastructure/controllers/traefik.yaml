---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik
      version: "34.*"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  values:
    # Use ClusterIP since Cloudflare Tunnel handles external access
    service:
      type: ClusterIP

    # Enable Traefik dashboard
    ingressRoute:
      dashboard:
        enabled: true
        matchRule: Host(`traefik.localhost`)
        entryPoints:
          - web

    # Standard Kubernetes ingress provider
    providers:
      kubernetesIngress:
        enabled: true
        publishedService:
          enabled: true

    # Entry points
    ports:
      web:
        port: 8000
        expose:
          default: true
        exposedPort: 80
        protocol: TCP
      websecure:
        port: 8443
        expose:
          default: true
        exposedPort: 443
        protocol: TCP
        # TLS terminated at Cloudflare, so we don't need it here
        tls:
          enabled: false

    # Logs
    logs:
      general:
        level: INFO
      access:
        enabled: true

    # Resources
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi
