apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Helm repositories (must be first)
  - sources
  # Storage (needed for PVCs)
  - storage
  # Ingress controller
  - controllers
  # Core infrastructure
  - cloudflare-tunnel
  # Observability stack
  - observability
