# SOPS configuration
# Generate age key: age-keygen -o keys.txt
# Store key at: ~/.config/sops/age/keys.txt

creation_rules:
  - path_regex: .*\.yaml$
    age: >-
      # Add your age public key here after generating
      # age1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
