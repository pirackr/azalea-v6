# Misty Bamboo VM Configuration
# Automatically loaded by Terraform

# SSH key will be loaded from file
# ssh_public_key is set via TF_VAR_ssh_public_key environment variable

# Tailscale key will be loaded from encrypted secret
# tailscale_authkey is set via TF_VAR_tailscale_authkey environment variable
